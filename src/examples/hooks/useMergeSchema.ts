import { ISchema } from "@formily/react"

export const useMergeSchema = (schema:ISchema, tabsSchema:ISchema):[ISchema] => {

    const mergeSchema:ISchema = {}
    
    // // console.log(tabsSchema.properties['collapse']. )

    return [mergeSchema]
}