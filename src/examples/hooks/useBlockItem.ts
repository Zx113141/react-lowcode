

export const useBlockItem = (currentBlock:any) => {

    let schema = {}
    let blockItem = {}

    // schema = currentBlock.schema?{}:currentBlock.schema
    // blockItem = currentBlock
    // updateBlocks([...,...blockItem])


    return [schema, blockItem]
}