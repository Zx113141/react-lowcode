import React from 'react'

interface BlocksStyle extends React.CSSProperties {
    id:string
    focus?:boolean
}


export const useBlocksStyle = (blocks:BlocksStyle[]) => {
    let blockStyles:BlocksStyle[] = []
    

    return blockStyles
}